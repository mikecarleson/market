  <section id="foods" class="container">
    <h1>Items for Sale</h1>

    {{#each foods}}
      <div class="food">
      <h3>{{item}}</h3>
      <p>{{description}}</p>
      <ul>
        <li>Price: {{price}}</li>
      </ul>
      <div class="loca">
        <small>Location: <span>{{technologies}}</span></small>
      </div>
    </div>
    {{else}}
      <p>No food</p>
    {{/each}}
    
  </section>